<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exibição de Dados</title>
    <link rel="stylesheet" href="tabelas.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap');

        * {
            font-family: 'Inter', sans-serif;
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            user-select: none;
        }

        body {
            padding: 1rem;
            height: 100vh;
            background-color: #F2F3F9;
            display: flex;
            justify-content: center
        }

        table {
            border-collapse: collapse;
            width: 80%;
            align-self: center;
            text-align: center;
        }

        caption {
            font-size: 1.3em;
            font-weight: bolder;
            padding-block: 1.2rem;
            color: #001489;

        }

        caption>div {
            display: flex;
            flex-direction: row;
            justify-content: space-between;
        }

        nav {
            display: flex;
            align-items: center;
            gap: .2rem;
        }

        a {
            background-color: #CCD0E7;
            color: #000;
            text-decoration: none;
            padding-inline: 1rem;
            padding-block: .3rem;
            border-radius: 10px;
            text-align: center;
            font-size: 1rem;
            font-weight: 500;
            border: none;
            transition: .4s;
            cursor: pointer;
        }

        a:hover {
            background-color: #8089C4;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f1f1f1;
        }

        th {
            background-color: #4C5AAC;
            color: white;
            font-weight: 500;
            font-size: 1rem;
        }

        tr {
            background-color: white;
        }

        th,
        td {
            border: 1px solid black;
            padding: 5px;
        }
    </style>
</head>

<body>
    <table>
        <caption>
            <div>
                Informações de presença
                <nav>
                    <a href="ausentes" target="_self">Conhecer candidatos ausentes</a>
                    <a href="presentes" target="_self">Conhecer candidatos presentes</a>
                </nav>
            </div>

        </caption>
        <thead>
            <tr>
                <th>Turmas</th>
                <th>Presentes</th>
                <th>Ausentes</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            <% info.forEach(function(row) { %>
                <tr>
                    <td>
                        <%= row.turma %>
                    </td>
                    <td>
                        <%= row.presentes %>
                    </td>
                    <td>
                        <%= row.ausentes %>
                    </td>
                    <td>
                        <%= row.total %>
                    </td>
                    <% }); %>
                </tr>


        </tbody>
        <tr></tr>
    </table>

</body>

</html>